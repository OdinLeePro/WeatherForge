# Additional Context Note

Files in this folder were copied from the source `additional docs` folder as supporting course context. They may reference the older working-project layout, including paths such as `data/linexandspectrogramprograms/processed/`.

For the cleaned final submission, use the root `README.md`, `data_dictionary.md`, `app.py`, `data/`, and `pipeline/` folders as the authoritative structure.
